services:
  npm-download-service:
    build: .
    stdin_open: true   # keeps stdin open (-i)
    tty: true          # allocates a TTY (-t) — required for interactive prompts
    volumes:
      - ./input:/app/input
      - ./output:/app/output